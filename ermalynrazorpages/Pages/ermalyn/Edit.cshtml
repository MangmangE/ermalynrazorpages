@page
@model ermalynrazorpages.Pages.ermalyn.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>christiansongs</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="christiansongs.ID" />
            <div class="form-group">
                <label asp-for="christiansongs.Title" class="control-label"></label>
                <input asp-for="christiansongs.Title" class="form-control" />
                <span asp-validation-for="christiansongs.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="christiansongs.ReleaseDate" class="control-label"></label>
                <input asp-for="christiansongs.ReleaseDate" class="form-control" />
                <span asp-validation-for="christiansongs.ReleaseDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="christiansongs.Genre" class="control-label"></label>
                <input asp-for="christiansongs.Genre" class="form-control" />
                <span asp-validation-for="christiansongs.Genre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="christiansongs.Price" class="control-label"></label>
                <input asp-for="christiansongs.Price" class="form-control" />
                <span asp-validation-for="christiansongs.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
